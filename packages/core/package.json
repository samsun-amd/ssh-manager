{
  "name": "@ssh-manager/core",
  "version": "0.1.0",
  "description": "Shared SSH inventory/connection/SFTP/transfer library extracted from sshm, consumed by md-reader and webscp.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch",
    "clean": "rm -rf dist",
    "test": "node --test \"test/**/*.test.js\""
  },
  "dependencies": {
    "ssh2": "^1.16.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "@types/ssh2": "^1.15.0",
    "typescript": "^5.7.0"
  }
}
